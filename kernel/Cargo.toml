[package]
name = "kernel"
version = "0.1.0"
edition = "2024"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
lazy_static = { version = "1.5.0", features = ["spin_no_std"] }
riscv = "0.14.0"
sbi-rt = "0.0.3"
